name: hubs

services:
  db:
    image: postgres:14-alpine
    container_name: hubs-db
    restart: unless-stopped
    cpu_shares: 70
    env_file:
      - /DATA/AppData/hubs/secrets.env
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    expose:
      - 5432
    volumes:
      - /DATA/AppData/hubs/pgdata:/var/lib/postgresql/data
    networks:
      - hubs-net

  reticulum:
    image: hubsfoundation/reticulum:stable-855
    container_name: hubs-reticulum
    restart: unless-stopped
    cpu_shares: 70
    env_file:
      - /DATA/AppData/hubs/secrets.env
    environment:
      POD_IP: "127.0.0.1"
      POD_NAME: "reticulum-1"
      turkeyCfg_POD_NS: "hubs"
      turkeyCfg_HUB_DOMAIN: "hubs-${APP_DOMAIN}"
      turkeyCfg_DOMAIN: "hubs-${APP_DOMAIN}"
      turkeyCfg_RET_HOST: "hubs-${APP_DOMAIN}"
      turkeyCfg_ASSETS_HOST: "hubsassets-${APP_DOMAIN}"
      turkeyCfg_DIALOG_HOST: "hubsdialog-${APP_DOMAIN}"
      turkeyCfg_NEARSPARK_HOST: "hubsnearspark-${APP_DOMAIN}"
      turkeyCfg_DB_HOST: "db"
      turkeyCfg_DB_HOST_T: "db"
      turkeyCfg_ADM_EMAIL: "${APP_EMAIL}"
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    expose:
      - 4000
      - 4001
    volumes:
      - /DATA/AppData/hubs/retstorage:/storage
      - /DATA/AppData/hubs/config.toml.template:/home/ret/config.toml.template:ro
    depends_on:
      db:
        condition: service_healthy
    networks:
      - hubs-net
      - pcs
    labels:
      caddy_0: hubs-${APP_DOMAIN}
      caddy_0.import: gateway_tls
      caddy_0.reverse_proxy: "{{upstreams 4001}}"
      caddy_1: hubs-${APP_PUBLIC_IP_DASH}.nip.io
      caddy_1.import: gateway_tls
      caddy_1.reverse_proxy: "{{upstreams 4001}}"
      caddy_2: hubs-${APP_PUBLIC_IP_DASH}.sslip.io
      caddy_2.reverse_proxy: "{{upstreams 4001}}"

  postgrest:
    image: mozillareality/postgrest:8
    container_name: hubs-postgrest
    restart: unless-stopped
    cpu_shares: 50
    env_file:
      - /DATA/AppData/hubs/secrets.env
    environment:
      PGRST_LOG_LEVEL: info
      PGRST_DB_SCHEMA: ret0_admin
      PGRST_DB_ANON_ROLE: postgres
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    expose:
      - 3000
    depends_on:
      db:
        condition: service_healthy
    networks:
      - hubs-net

  hubs-client:
    image: hubsfoundation/hubs:stable-3111
    container_name: hubs-client
    restart: unless-stopped
    cpu_shares: 80
    environment:
      DOMAIN: "${APP_DOMAIN}"
      SUB_DOMAIN: "hubs"
      turkeyCfg_thumbnail_server: "hubsnearspark-${APP_DOMAIN}"
      turkeyCfg_base_assets_path: "https://hubsassets-${APP_DOMAIN}/hubs/"
      turkeyCfg_non_cors_proxy_domains: "hubs-${APP_DOMAIN},hubsassets-${APP_DOMAIN}"
      turkeyCfg_reticulum_server: "hubs-${APP_DOMAIN}"
      turkeyCfg_cors_proxy_server: "hubs-${APP_DOMAIN}"
      turkeyCfg_shortlink_domain: "hubs-${APP_DOMAIN}"
      turkeyCfg_tier: "p1"
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    expose:
      - 8080
    networks:
      - hubs-net
      - pcs
    labels:
      caddy_0: hubsassets-${APP_DOMAIN}
      caddy_0.import: gateway_tls
      caddy_0.reverse_proxy: "https://{{upstreams 8080}}"
      caddy_0.reverse_proxy.transport: http
      caddy_0.reverse_proxy.transport.tls: ""
      caddy_0.reverse_proxy.transport.tls_insecure_skip_verify: ""
      caddy_1: hubsassets-${APP_PUBLIC_IP_DASH}.nip.io
      caddy_1.import: gateway_tls
      caddy_1.reverse_proxy: "https://{{upstreams 8080}}"
      caddy_1.reverse_proxy.transport: http
      caddy_1.reverse_proxy.transport.tls: ""
      caddy_1.reverse_proxy.transport.tls_insecure_skip_verify: ""

  spoke:
    image: hubsfoundation/spoke:stable-95
    container_name: hubs-spoke
    restart: unless-stopped
    cpu_shares: 50
    environment:
      DOMAIN: "${APP_DOMAIN}"
      SUB_DOMAIN: "hubsspoke"
      turkeyCfg_thumbnail_server: "hubsnearspark-${APP_DOMAIN}"
      turkeyCfg_base_assets_path: "https://hubsassets-${APP_DOMAIN}/spoke/"
      turkeyCfg_non_cors_proxy_domains: "hubs-${APP_DOMAIN},hubsassets-${APP_DOMAIN}"
      turkeyCfg_reticulum_server: "hubs-${APP_DOMAIN}"
      turkeyCfg_cors_proxy_server: "hubs-${APP_DOMAIN}"
      turkeyCfg_shortlink_domain: "hubs-${APP_DOMAIN}"
      turkeyCfg_hubs_server: "hubs-${APP_DOMAIN}"
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    expose:
      - 8080
    networks:
      - hubs-net
      - pcs
    labels:
      caddy_0: hubsspoke-${APP_DOMAIN}
      caddy_0.import: gateway_tls
      caddy_0.reverse_proxy: "https://{{upstreams 8080}}"
      caddy_0.reverse_proxy.transport: http
      caddy_0.reverse_proxy.transport.tls: ""
      caddy_0.reverse_proxy.transport.tls_insecure_skip_verify: ""
      caddy_1: hubsspoke-${APP_PUBLIC_IP_DASH}.nip.io
      caddy_1.import: gateway_tls
      caddy_1.reverse_proxy: "https://{{upstreams 8080}}"
      caddy_1.reverse_proxy.transport: http
      caddy_1.reverse_proxy.transport.tls: ""
      caddy_1.reverse_proxy.transport.tls_insecure_skip_verify: ""

  dialog:
    image: hubsfoundation/dialog:stable-331
    container_name: hubs-dialog
    restart: unless-stopped
    cpu_shares: 70
    environment:
      AUTH_KEY: /app/certs/perms.pub.pem
      HTTPS_CERT_FULLCHAIN: /app/certs/fullchain.pem
      HTTPS_CERT_PRIVKEY: /app/certs/privkey.pem
      INTERACTIVE: "false"
      MEDIASOUP_MIN_PORT: 40000
      MEDIASOUP_MAX_PORT: 40050
      MEDIASOUP_ANNOUNCED_IP: "${APP_PUBLIC_IP}"
      DEBUG: "*INFO* *WARN* *ERROR*"
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    volumes:
      - /DATA/AppData/hubs/perms.pub.pem:/app/certs/perms.pub.pem:ro
    command: ["node", "index.js"]
    expose:
      - 4443
    ports:
      - "40000-40050:40000-40050/udp"
      - "40000-40050:40000-40050/tcp"
    networks:
      - hubs-net
      - pcs
    labels:
      caddy_0: hubsdialog-${APP_DOMAIN}
      caddy_0.import: gateway_tls
      caddy_0.reverse_proxy: "https://{{upstreams 4443}}"
      caddy_0.reverse_proxy.transport: http
      caddy_0.reverse_proxy.transport.tls: ""
      caddy_0.reverse_proxy.transport.tls_insecure_skip_verify: ""
      caddy_1: hubsdialog-${APP_PUBLIC_IP_DASH}.nip.io
      caddy_1.import: gateway_tls
      caddy_1.reverse_proxy: "https://{{upstreams 4443}}"
      caddy_1.reverse_proxy.transport: http
      caddy_1.reverse_proxy.transport.tls: ""
      caddy_1.reverse_proxy.transport.tls_insecure_skip_verify: ""

  nearspark:
    image: hubsfoundation/nearspark:stable-28
    container_name: hubs-nearspark
    restart: unless-stopped
    cpu_shares: 30
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    expose:
      - 5000
    networks:
      - hubs-net
      - pcs
    labels:
      caddy_0: hubsnearspark-${APP_DOMAIN}
      caddy_0.import: gateway_tls
      caddy_0.reverse_proxy: "{{upstreams 5000}}"
      caddy_1: hubsnearspark-${APP_PUBLIC_IP_DASH}.nip.io
      caddy_1.import: gateway_tls
      caddy_1.reverse_proxy: "{{upstreams 5000}}"

  photomnemonic:
    image: hubsfoundation/photomnemonic:stable-75
    container_name: hubs-photomnemonic
    restart: unless-stopped
    cpu_shares: 30
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    expose:
      - 5000
    networks:
      - hubs-net

networks:
  hubs-net:
    name: hubs-net
  pcs:
    name: pcs
    external: true

x-casaos:
  architectures:
    - amd64
  main: reticulum
  webui_port: 4001
  port_map: "4001"
  index: "/"
  scheme: http

  author: Hubs Foundation
  developer: Hubs Foundation
  category: Communication

  icon: https://cdn.jsdelivr.net/gh/Worph/AppStore@main/Apps/Hubs/icon.png
  thumbnail: https://cdn.jsdelivr.net/gh/Worph/AppStore@main/Apps/Hubs/thumbnail.png
  screenshot_link:
    - https://cdn.jsdelivr.net/gh/Worph/AppStore@main/Apps/Hubs/screenshot-1.png
    - https://cdn.jsdelivr.net/gh/Worph/AppStore@main/Apps/Hubs/screenshot-2.png
    - https://cdn.jsdelivr.net/gh/Worph/AppStore@main/Apps/Hubs/screenshot-3.png

  title:
    en_us: Hubs

  tagline:
    en_us: Self-hosted social VR rooms in your browser

  description:
    en_us: |
      Hubs is an open-source social VR platform from the Hubs Foundation. Create persistent virtual rooms for meetings, classes, events, or hangouts — accessible from any browser, no install required, with full WebXR support for VR headsets.

      This deployment runs the full Hubs Foundation stable stack: Reticulum (Phoenix backend), Hubs (3D client), Spoke (scene editor), Dialog (Mediasoup WebRTC SFU), Nearspark (thumbnail proxy), Photomnemonic (page screenshotter), PostgreSQL, and PostgREST.

      First boot generates an RSA permissions keypair and a set of Phoenix/Guardian/PostgREST secrets in /DATA/AppData/hubs/secrets.env. The same secrets are reused on subsequent boots.

  pre-install-cmd: |
    mkdir -p /DATA/AppData/hubs/
    if [ ! -f /DATA/AppData/hubs/.initialized ]; then
      wget -qO /DATA/AppData/hubs/install.sh https://raw.githubusercontent.com/Worph/AppStore/main/Apps/Hubs/pre-install/install.sh
      chmod +x /DATA/AppData/hubs/install.sh
      APP_DEFAULT_PASSWORD="$APP_DEFAULT_PASSWORD" PUID="$PUID" PGID="$PGID" bash /DATA/AppData/hubs/install.sh
      touch /DATA/AppData/hubs/.initialized
    fi

  tips:
    before_install:
      en_us: |
        Hubs is a multi-service stack. It exposes five public hostnames:

        - hubs-${APP_DOMAIN} — main app, room URLs, login (Reticulum)
        - hubsassets-${APP_DOMAIN} — static assets (3D models, JS bundles)
        - hubsspoke-${APP_DOMAIN} — Spoke scene editor
        - hubsdialog-${APP_DOMAIN} — WebRTC signaling (WSS, port 4443)
        - hubsnearspark-${APP_DOMAIN} — thumbnail proxy

        Voice/video requires UDP ports 40000-40050 to be reachable from clients on this server's public IP (APP_PUBLIC_IP). If the PCS sits behind a tunnel that only forwards HTTPS, signaling will work but media will fail. Verify the firewall allows inbound UDP 40000-40050.

        Resource footprint: ~3 GB RAM minimum, ~2 vCPU. Suitable for small rooms (<10 concurrent users).

        Storage: Postgres data at /DATA/AppData/hubs/pgdata, room uploads at /DATA/AppData/hubs/retstorage, secrets at /DATA/AppData/hubs/secrets.env (chmod 600).

        After install, sign in at https://hubs-${APP_DOMAIN} using ${APP_EMAIL}; an admin login link is mailed (or visible in Reticulum logs if SMTP is unconfigured).
